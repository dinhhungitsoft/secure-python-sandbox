# Execution mode
# Options: simple, secure
EXECUTION_MODE=secure

# Default timeout for code execution (seconds)
# Range: 1-300
SANDBOX_TIMEOUT=30

# Allow network access by default
# Values: true, false, 1, 0, yes, no
SANDBOX_ALLOW_NETWORK=false
